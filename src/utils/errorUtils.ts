// Handle HTTP errors by sending a JSON response with the specified status code and message.

// export const handleErrors = (
//   res: Response,
//   statusCode: number,
//   message: string,
// ): void => {
//   res.status(statusCode).json({ message });
// };
